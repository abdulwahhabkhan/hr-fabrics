export const ConfirmTransactionDate = ({transaction_date, callback}) => {
    const handleConfirm = () => {
        callback(transaction_date)
    }
    return (
        <div>

        </div>
    )
}
