/**
 * Application layout switch (same pattern as the Laravel React starter kit).
 *
 * Every page resolves to this file (see app.tsx), so the whole app's chrome is
 * chosen by the single import below:
 *
 *   - Sidebar layout (current): './app/AppSidebarLayout.jsx'
 *   - Legacy top-menu layout:  './app/AppHeaderLayout.jsx'
 */
export { default } from './app/AppSidebarLayout.jsx';
